# DevNovate Blog Platform - Environment Variables Template
# Copy this file to .env and fill in your actual values

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# MongoDB connection string from MongoDB Atlas
MONGODB_URI=mongodb+srv://devnovate-admin:YOUR_PASSWORD@devnovate-cluster.xxxxx.mongodb.net/devnovate-blog?retryWrites=true&w=majority

# ===========================================
# JWT AUTHENTICATION
# ===========================================
# Secret key for JWT token signing (use a strong random string in production)
JWT_SECRET=devnovate-super-secret-jwt-key-for-vibe-hack-2025-bit-durg-students
# Token expiration time
JWT_EXPIRE=7d

# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Port for the Express server
PORT=5001
# Environment mode
NODE_ENV=development

# ===========================================
# SECURITY CONFIGURATION (Optional)
# ===========================================
# CORS allowed origins (automatically configured in code)
# CORS_ORIGIN=https://your-frontend-domain.com

# Rate limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# EMAIL CONFIGURATION (Future feature)
# ===========================================
# Email service for notifications (not implemented yet)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# ===========================================
# FILE UPLOAD (Future feature)
# ===========================================
# Cloudinary or AWS S3 for image uploads (not implemented yet)
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret

# ===========================================
# DEPLOYMENT NOTES
# ===========================================
# For Railway.app deployment:
# 1. Add these variables in Railway dashboard
# 2. Remove .env.example from .gitignore temporarily if needed
# 3. Never commit actual .env files with real credentials!
# 
# For local development:
# 1. Copy this file to .env
# 2. Replace YOUR_PASSWORD with your actual MongoDB Atlas password
# 3. Update other values as needed
